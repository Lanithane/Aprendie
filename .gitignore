node_modules/
dist/
dist-server/
build/
.vite/
coverage/

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# claude code local settings (project-shared settings.json may be committed)
.claude/settings.local.json
# claude code transient runtime state (session locks, etc.)
.claude/scheduled_tasks.lock

# drizzle
drizzle/meta/_journal.json.lock

# build-time asset cache (gen:og downloads the brand TTF here; PNGs in public/ are committed)
scripts/.assets/
